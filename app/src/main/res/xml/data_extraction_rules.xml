<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude model files from cloud backup -->
        <exclude domain="file" path="models/" />
    </cloud-backup>
    <device-transfer>
        <!-- Include app data in device transfer -->
        <include domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
